spring:
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema-mysql.sql

knowledge:
  cache:
    provider: redis
  embedding:
    provider: hash
  storage:
    provider: minio
    minio:
      endpoint: http://localhost:9000
      bucket: fast-knowledge
      access-key: minioadmin
      secret-key: minioadmin
      prefix: knowledge/
  llm:
    provider: ollama
    allow-external: false
