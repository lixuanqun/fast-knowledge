<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fast.knowledge.mapper.SystemConfigMapper">

    <select id="getValue" resultType="string">
        SELECT config_value FROM kb_system_config WHERE config_key = #{key}
    </select>

    <insert id="upsert">
        INSERT INTO kb_system_config (config_key, config_value)
        VALUES (#{key}, #{value})
        ON DUPLICATE KEY UPDATE config_value = #{value}
    </insert>
</mapper>
